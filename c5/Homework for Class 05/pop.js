// The Array pop() method will remove the last item from an Array and it will return it

const numbers = [1, 2, 3, 4, 5];

numbers.pop();

// It can also return it if we store in a variable
const lastDeletedItem = numbers.pop();

console.log(numbers);
console.log(lastDeletedItem);
