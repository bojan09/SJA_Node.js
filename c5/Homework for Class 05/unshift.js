// The Array unshift() method add one or more elements at the beggining of the array

const numbers = [1, 2, 3, 4, 5];

// We can also store the total length in a variable
const totalNum = numbers.unshift(0, -1);

console.log(numbers);
console.log(totalNum);
