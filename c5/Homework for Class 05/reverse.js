// The reverse() Array method is the opposite of sort(), it will display the last name first.

// Example 1 - Sorting an Array of strigns, containing names
const names = ["John", "Lucas", "Anna", "Rambo", "Bond", "Zelda"];

names.reverse();

console.log(names);
